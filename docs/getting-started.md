# Getting started with pumpfun-creator-rewards

Look up Pump.fun creator-fee earnings by coin mint, wallet, or username.

## Install

```bash
npm install
```

## Verify the install

Clone the repository and run its checks to confirm everything works on your machine:

```bash
git clone https://github.com/nirholas/pumpfun-creator-rewards.git
cd pumpfun-creator-rewards
```

Available commands:

| Command | Runs |
|---|---|
| `npm run dev` | `vite` |
| `npm run build` | `vite build` |
| `npm run start` | `node server.js` |

## Next steps

- [Examples](./examples.md) shows runnable snippets.
- The [README](https://github.com/nirholas/pumpfun-creator-rewards#readme) is the complete reference.
- Found a problem? [Open an issue](https://github.com/nirholas/pumpfun-creator-rewards/issues).
