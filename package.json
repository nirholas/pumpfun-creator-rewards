{
  "name": "pumpfun-creator-fees",
  "version": "0.1.0",
  "private": true,
  "description": "Look up Pump.fun creator-fee earnings by coin mint, wallet, or username.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "serve": "npm run build && node server.js",
    "deploy": "gcloud builds submit --config cloudbuild.yaml --region us-central1 --project aerial-vehicle-466722-p5"
  },
  "dependencies": {
    "sirv": "^3.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
